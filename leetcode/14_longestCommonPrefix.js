// input: given an array of strings
// output: a string of longest common prefix, if not found return empty string

// create a res empty string
// loop thru the array of strings based on the length of first string
    // iterate thru all strings make sure all strings have the same char at index i
        //
