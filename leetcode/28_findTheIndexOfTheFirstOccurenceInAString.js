// input: 2 strings: needle and haystack
// output: return the index of first occurence of needle in haystack. If not present, return -1

var strStr = function(haystack, needle) {
    return haystack.indexOf(needle);
};
